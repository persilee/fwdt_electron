{
  "name": "fwdt_electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager ./ FWDTElectron --all --out ./OutApp --overwrite --ignore=node_modules --icon=./notice_board/images/app.ico",
    "packagerWin": "electron-packager ./ FWDTElectron --win32 --arch=x64 --out ./OutApp --arch=x64 --overwrite --ignore=node_modules --icon=./notice_board/images/app.ico"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "devtron": "^1.4.0",
    "electron-packager": "^13.1.0",
    "electron-reload": "^1.4.0"
  },
  "dependencies": {
    "electron": "^4.0.5"
  }
}
